# Same value Amplify Hosting sets as env API_BASE_URL (amplify.yml writes web/api-config.js at build).
# Locally: edit web/api-config.js (empty base = UI shows config hint until you set the URL).
API_BASE_URL=https://YOUR_API_ID.execute-api.ap-southeast-1.amazonaws.com
